# requirements.txt
gevent>=21.8.0
flask>=2.3.3
markupsafe>=2.1.3
websockets>=11.0.3
dashscope>=1.14.0
regex>=2023.10.3

# 移除 threading 和 asyncio（它们是 Python 标准库）
# threading 是 Python 内置模块，不需要安装
# asyncio 是 Python 3.4+ 内置模块，不需要安装

# 如果 flask-sockets 不可用，使用 flask-socketio 替代
flask-socketio>=5.3.0

# greenlet 是 gevent 的依赖
greenlet>=2.0.0
